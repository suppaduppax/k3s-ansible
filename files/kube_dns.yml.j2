apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-dns
  namespace: kube-system
data:
  upstreamNameservers: |
    [{% for dns in kube_dns_nameservers %}"{{ dns }}"{{',' if loop.index < ( kube_dns_nameservers | length ) - 1 else '' }}{% endfor %}]
